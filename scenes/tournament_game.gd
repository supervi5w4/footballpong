# ------------------------------------------------------------
# tournament_game.gd — Проведение матча в турнире
# Динамически настраивает ИИ по силе соперника и ходу игры
# Управляет двумя таймами и итогом
# Интегрирован с табло времени
# ------------------------------------------------------------

extends Game

# Турнирная версия игры
# Наследует всю логику от game.gd, но добавляет турнирную функциональность

func _ready():
	# Инициализация турнирного контроллера
	Score.reset_score()
	
	# Вызываем родительскую логику
	super._ready()
